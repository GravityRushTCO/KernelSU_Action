CONFIG_ENV=config.env

# SOURCE : On utilise les sources du kernel de ton modèle
KERNEL_SOURCE=https://github.com/KSU-Build/android_kernel_samsung_sm8250
KERNEL_SOURCE_BRANCH=master
KERNEL_CONFIG=g781b_defconfig
KERNEL_IMAGE_NAME=Image
ARCH=arm64
ADD_LOCALVERSION_TO_FILENAME=false
EXTRA_CMDS:LD=ld.lld

# Clang (On reste sur AOSP pour la stabilité avec Android 13)
USE_CUSTOM_CLANG=false
CUSTOM_CLANG_SOURCE=
CUSTOM_CLANG_BRANCH=
CUSTOM_CMDS:CLANG_TRIPLE=aarch64-linux-gnu-

## AOSP
CLANG_BRANCH=master-kernel-build-2022
CLANG_VERSION=r450784e

# GCC (Nécessaire pour les kernels Samsung 4.19)
ENABLE_GCC_ARM64=true
ENABLE_GCC_ARM32=true
USE_CUSTOM_GCC_64=false
CUSTOM_GCC_64_SOURCE=
CUSTOM_GCC_64_BRANCH=
CUSTOM_GCC_64_BIN=aarch64-linux-android-
USE_CUSTOM_GCC_32=false
CUSTOM_GCC_32_SOURCE=
CUSTOM_GCC_32_BRANCH=
CUSTOM_GCC_32_BIN=arm-linux-androideabi-

# KernelSU flags (C'est ici que tu actives la magie)
ENABLE_KERNELSU=true
KERNELSU_TAG=main
KSU_EXPECTED_SIZE=
KSU_EXPECTED_HASH=

# Configuration (Très important pour éviter les crashs)
DISABLE-LTO=true
DISABLE_CC_WERROR=true
ADD_KPROBES_CONFIG=true
ADD_OVERLAYFS_CONFIG=true
APPLY_KSU_PATCH=true
REMOVE_UNUSED_PACKAGES=true

# AnyKernel3 (Pour créer un zip flashable si besoin)
USE_CUSTOM_ANYKERNEL3=false
CUSTOM_ANYKERNEL3_SOURCE=
CUSTOM_ANYKERNEL3_BRANCH=

# Ccache
ENABLE_CCACHE=true

# DTBO image (Samsung en a souvent besoin)
NEED_DTBO=false

# Build boot images (C'est ton fichier source que tu as uploadé)
BUILD_BOOT_IMG=true
SOURCE_BOOT_IMAGE=https://files.catbox.moe/w4k8ug.img
